FROM alpine:latest
WORKDIR /app

# Copy only the pre-built binary from GoReleaser (frontend already embedded)
COPY mnlrbase /app/mnlrbase
RUN chmod +x /app/mnlrbase

EXPOSE 8090

CMD ["/app/mnlrbase", "serve", "--http=0.0.0.0:8090"]
